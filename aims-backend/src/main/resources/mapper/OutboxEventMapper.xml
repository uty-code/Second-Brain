<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aimsgraph.outbox.OutboxEventMapper">

    <insert id="insert">
        INSERT INTO OutboxEvent (
            id, workspace_id, aggregate_type, aggregate_id, event_type, payload, status, created_at
        ) VALUES (
            #{id}, #{workspaceId}, #{aggregateType}, #{aggregateId}, #{eventType}, #{payload}, #{status}, GETUTCDATE()
        )
    </insert>

    <select id="findByWorkspaceId" resultType="com.aimsgraph.outbox.OutboxEvent">
        SELECT *
        FROM OutboxEvent
        WHERE workspace_id = #{workspaceId}
    </select>

    <select id="findPendingEvents" resultType="com.aimsgraph.outbox.OutboxEvent">
        SELECT *
        FROM OutboxEvent
        WHERE status = 'PENDING'
        ORDER BY created_at ASC
    </select>

    <update id="updateStatus">
        UPDATE OutboxEvent
        SET status = #{status}
        WHERE id = #{id}
    </update>

</mapper>
