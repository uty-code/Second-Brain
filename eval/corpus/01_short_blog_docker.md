# Docker 컨테이너의 기본 개념

Docker 컨테이너는 애플리케이션과 이를 실행하는 데 필요한 모든 라이브러리, 의존성을 하나의 독립된 패키지로 묶어주는 기술입니다.
기존의 가상 머신(VM)과 달리 별도의 게스트 OS를 설치하지 않고, 호스트 운영체제의 리눅스 커널을 격리된 프로세스 형태로 공유합니다.
이를 통해 수 초 만에 시작할 수 있는 가벼움과 뛰어난 이식성을 제공합니다.
개발 환경과 운영 환경의 일치성을 보장하여 "내 컴퓨터에서는 잘 됐는데 서버에서는 안 된다"는 환경 불일치 문제를 근본적으로 해결합니다.
